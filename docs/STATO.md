# Stato del progetto

> Questa è la bacheca condivisa. Ogni agente la legge all'avvio per capire
> dove siamo, e la aggiorna quando finisce qualcosa.
>
> È il sostituto del "manager di reparto": invece di un agente che coordina
> altri agenti, c'è un file che tutti leggono. Costa zero token, non sbaglia,
> e lo puoi leggere anche tu.

## Fase corrente

`BRIEF` — in attesa del brief del cliente.

<!-- Fasi: BRIEF → PRD → DESIGN+CONTRATTO → BUILD → QA → RILASCIO -->

## Checkpoint

| # | Cosa | Stato |
|---|------|-------|
| 1 | PRD approvato | ☐ |
| 2 | Design system + contratto API approvati | ☐ |
| 3 | Test verdi, pronto al rilascio | ☐ |

## Sbloccati adesso

Nessuno. In attesa del brief.

<!--
  Qui si scrive chi può partire e su cosa. Esempio, dopo il checkpoint 2:

  - ● frontend  → implementa le schermate 1-4 di docs/flussi.md
  - ■ backend   → implementa gli endpoint /match e /event
  - ✱ qa        → scrivi gli e2e dei flussi 1 e 2

  Così apri il riquadro giusto, scrivi "procedi" e l'agente sa già cosa fare
  perché lo legge qui.
-->

## Bloccati

Nessuno.

<!--
  Chi non può procedere e perché. Esempio:
  - ● frontend → aspetta il campo `set_number` nel contratto API
-->

## Registro

<!-- Ultima riga in cima. Una riga per evento, con la data. -->

- _(vuoto)_
